server {
    listen 81;
    server_name localhost;

    location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        allow 172.16.0.0/12;  # Разрешаем доступ из Docker сети
        allow 192.168.0.0/16;
        deny all;
    }
}